interface:
  display_name: "Repo Resource Prepare"
  short_description: "Create repo env, then stage resources"
  default_prompt: "Use $repo-resource-prepare to create or confirm the per-repository virtual environment, then identify required resources and stage them into the run directory."
