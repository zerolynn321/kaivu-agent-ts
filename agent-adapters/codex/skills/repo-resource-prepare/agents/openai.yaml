interface:
  display_name: "Repo Resource Prepare"
  short_description: "Choose env, then stage resources"
  default_prompt: "Use $repo-resource-prepare to ask whether to reuse the current environment or create a new repo environment before downloading resources, then stage required resources."
