interface:
  display_name: "Repo Experiment Fix"
  short_description: "Adapt repos for the required comparison"
  default_prompt: "Use $repo-experiment-fix to expose the required baseline comparison branches before onboarding."
