interface:
  display_name: "Repo Environment Setup"
  short_description: "Build the minimum baseline environment"
  default_prompt: "Use $repo-environment-setup to install and validate only the dependencies required by the selected minimum reproduction, choosing CPU or accelerators from repository evidence and experiment fidelity rather than a CPU-first policy."
