interface:
  display_name: "Repo Environment Setup"
  short_description: "Validate active repo env and install deps"
  default_prompt: "Use $repo-environment-setup to verify commands target the repository-specific virtual environment, install approved dependencies there, and validate before baseline execution."
