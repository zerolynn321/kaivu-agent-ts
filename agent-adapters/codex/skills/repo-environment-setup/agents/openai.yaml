interface:
  display_name: "Repo Environment Setup"
  short_description: "Validate active repo env and install deps"
  default_prompt: "Use $repo-environment-setup to verify the current shell is inside the repository-specific virtual environment, install approved dependencies, and validate before baseline execution."
