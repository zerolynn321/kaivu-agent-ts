interface:
  display_name: "Benchmark Selection"
short_description: "Choose the minimum benchmark experiment"
default_prompt: "Use $benchmark-selection to choose one representative benchmark and only the control branches required to answer comparative research needs."
