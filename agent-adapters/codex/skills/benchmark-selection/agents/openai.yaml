interface:
  display_name: "Benchmark Selection"
  short_description: "Choose a claim-aligned minimum benchmark"
  default_prompt: "Use $benchmark-selection to choose the smallest core-valid benchmark, classify the core contribution, and require method execution plus a new artifact when the contribution generates or changes one."
