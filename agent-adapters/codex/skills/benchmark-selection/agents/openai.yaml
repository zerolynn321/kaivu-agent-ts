interface:
  display_name: "Benchmark Selection"
  short_description: "Choose a claim-aligned minimum benchmark"
  default_prompt: "Use $benchmark-selection to choose the smallest benchmark scope that fully satisfies the core-claim evidence gates, treating resource efficiency as secondary and rejecting smoke-only demos."
