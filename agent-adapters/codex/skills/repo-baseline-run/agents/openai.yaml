interface:
  display_name: "Repo Baseline Run"
  short_description: "Run the final minimum reproduction baseline"
  default_prompt: "Use $repo-baseline-run to reproduce the original method's minimum meaningful result and mark the repository ready for optimization."
