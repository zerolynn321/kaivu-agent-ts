interface:
  display_name: "Repo Baseline Run"
  short_description: "Run the final minimum baseline comparison"
  default_prompt: "Use $repo-baseline-run to run the configured minimum baseline or controlled comparison and mark the repository ready for optimization."
