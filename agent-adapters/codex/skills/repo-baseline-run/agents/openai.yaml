interface:
  display_name: "Repo Baseline Run"
short_description: "Run the final minimum baseline"
default_prompt: "Use $repo-baseline-run to run the configured minimum baseline experiment, including controlled comparison branches only when required, and mark the repository ready for optimization."
