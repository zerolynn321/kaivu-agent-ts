interface:
  display_name: "Repo Baseline Run"
  short_description: "Run the core-valid minimum baseline"
  default_prompt: "Use $repo-baseline-run to run one representative paper-aligned core experiment and mark readiness only when required resources, raw logs, raw metrics, outputs, and rerun assets are present and verified."
