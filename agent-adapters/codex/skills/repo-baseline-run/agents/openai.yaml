interface:
  display_name: "Repo Baseline Run"
  short_description: "Run the core-valid minimum baseline"
  default_prompt: "Use $repo-baseline-run to run the core-valid baseline; when method execution is required, run the claim-bearing stages, produce a new artifact, and evaluate it before marking readiness."
