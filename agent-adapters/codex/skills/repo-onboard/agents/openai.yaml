interface:
  display_name: "Repo Onboard"
  short_description: "Infer eval and baseline refs"
  default_prompt: "Use $repo-onboard to inspect this cloned repository, infer the eval command, identify documented baseline references, and ensure it has a local config.yaml."
