interface:
  display_name: "Repo Onboard"
  short_description: "Find the minimum original-method reproduction path"
  default_prompt: "Use $repo-onboard to configure a paper-aligned core experiment, treating official-checkpoint single-sample inference as demo-only for dataset-level claims."
