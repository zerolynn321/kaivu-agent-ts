interface:
  display_name: "Research Experiment Init"
  short_description: "Prepare a minimal reproducible optimization repository"
  default_prompt: "Use $research-experiment-init to prepare a repository whose minimum scope reproduces the core experiment; for dataset-level claims require representative benchmark evaluation rather than demo inference."
