interface:
  display_name: "Research Experiment Init"
  short_description: "Route a research need to full baseline init"
  default_prompt: "Use $research-experiment-init to turn this research need into a runnable local baseline initialization."
